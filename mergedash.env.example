# Copy to mergedash.env, fill in real values, then:
#   chown mergedash:mergedash mergedash.env
#   chmod 600 mergedash.env
# Do NOT commit the filled-in file.

GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
PG_DSN=postgresql://user1:REPLACE_WITH_PASSWORD@localhost:5432/gitlabmr
